const allElements = document.querySelectorAll('a','button');
console.log(allElements);

// // Select all <button> and <a> elements
// const elements = document.querySelectorAll('button, a');

// // Iterate over the NodeList using a loop
// for (let i = 0; i < elements.length; i++) {
//     const element = elements[i];
//     console.log(element.tagName); // Output the tag name of each element
// }

// // Alternatively, use forEach to iterate over the NodeList (requires conversion)
// Array.from(elements).forEach(element => {
//     console.log(element.textContent); // Output the text content of each element
// });
